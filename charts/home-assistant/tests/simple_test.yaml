suite: Simple Test Suite
templates:
- deployment.yaml
chart:
  version: 0.1.0
  appVersion: 1.2.3
tests:
- it: Matches the snapshot
  asserts:
  - matchSnapshot: {}
- it: has config
  set:
    persistence:
      enabled: true
      config:
        enabled: true
  asserts:
  - matchSnapshot: {}
